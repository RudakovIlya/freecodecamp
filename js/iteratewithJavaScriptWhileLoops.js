//Name of the task: Iterate with JavaScript While Loops

//task: Add the numbers 5 through 0 (inclusive) in descending order to myArray using a while loop.

//solution:

const myArray = [];

let iteration = 5;

while (iteration >= 0) {
	myArray.push(iteration);
	iteration--;
}
