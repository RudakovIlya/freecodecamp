//Name of the task: Use the parseInt Function

//task: Use parseInt() in the convertToInteger function so it converts the input string str into an integer, and returns it.

//solution:

function convertToInteger(str) {

	return parseInt(str)

}

convertToInteger("56");