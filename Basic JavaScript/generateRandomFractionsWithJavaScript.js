//Name of the task: Generate Random Fractions with JavaScript

//task: Change randomFraction to return a random number instead of returning 0.

//solution:

function randomFraction() {
    return Math.random();
}
